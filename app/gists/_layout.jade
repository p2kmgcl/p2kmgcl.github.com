doctype
html(lang="es" class="section_" + current.path[0] data-section=current.path[0])
  head
    != partial('../_partials/_head')
  body
    != partial('../_partials/_menu')
    #wrapper
        article.gist
            h2.sectionTitle= title
            != yield
    != partial('../_partials/_footer')
