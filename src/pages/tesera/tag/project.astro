---
import TeseraEntryList from '../../../components/TeseraEntryList.astro';
import Base from '../../../layouts/Base.astro';
import pkg from '../../../../package.json';

export const tag = 'project';
export const emoji = '🧑‍🎓';
export const label = 'Projects';

const title = `${pkg.config.blogName}${pkg.config.blogTagSeparator}${label}`;
---

<Base
  title={title}
  documentAttributes={{
    'data-page': 'tesera-index',
    [`data-tesera-tag--${tag}`]: true,
  }}
>
  <TeseraEntryList emoji={emoji} label={label} tag={tag} />
</Base>
